# Example Code for using the `gym-idsgame` RL Environment

The original environment can be found here: https://github.com/Limmen/gym-idsgame

## Installation
 
```bash
pip install gym-idsgame==1.0.12
```